import React from 'react'

import links from '../../constants/links'
import './style.module.scss'

const Menu = () => {
    return (
      <div>
        Menu
        {/* {links.map(link => {
            return <p key={link.id}>{link.path}</p>
        })} */}
      </div>
    )
}

export default Menu
